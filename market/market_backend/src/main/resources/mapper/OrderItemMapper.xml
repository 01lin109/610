<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.market.mapper.OrderItemMapper">
    <resultMap id="BaseResultMap" type="com.market.entity.OrderItem">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="order_no" property="orderNo"/>
        <result column="product_id" property="productId"/>
        <result column="product_name" property="productName"/>
        <result column="product_image" property="productImage"/>
        <result column="product_price" property="productPrice"/>
        <result column="quantity" property="quantity"/>
        <result column="total_amount" property="totalAmount"/>
    </resultMap>
    <sql id="Base_Column_List">id, order_id, order_no, product_id, product_name, product_image, product_price, quantity, total_amount</sql>
    <select id="selectById" resultMap="BaseResultMap">SELECT <include refid="Base_Column_List"/> FROM order_item WHERE id = #{id}</select>
    <select id="selectByOrderId" resultMap="BaseResultMap">SELECT <include refid="Base_Column_List"/> FROM order_item WHERE order_id = #{orderId}</select>
    <select id="selectAll" resultMap="BaseResultMap">SELECT <include refid="Base_Column_List"/> FROM order_item</select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO order_item (order_id, order_no, product_id, product_name, product_image, product_price, quantity, total_amount)
        VALUES (#{orderId}, #{orderNo}, #{productId}, #{productName}, #{productImage}, #{productPrice}, #{quantity}, #{totalAmount})
    </insert>
    <insert id="insertBatch">
        INSERT INTO order_item (order_id, order_no, product_id, product_name, product_image, product_price, quantity, total_amount) VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.orderId}, #{item.orderNo}, #{item.productId}, #{item.productName}, #{item.productImage}, #{item.productPrice}, #{item.quantity}, #{item.totalAmount})
        </foreach>
    </insert>
    <update id="update">
        UPDATE order_item SET order_id=#{orderId}, order_no=#{orderNo}, product_id=#{productId}, product_name=#{productName}, product_image=#{productImage}, product_price=#{productPrice}, quantity=#{quantity}, total_amount=#{totalAmount} WHERE id=#{id}
    </update>
    <delete id="deleteById">DELETE FROM order_item WHERE id = #{id}</delete>
    <delete id="deleteByOrderId">DELETE FROM order_item WHERE order_id = #{orderId}</delete>
</mapper>